# Deprecated — merged into dotfiles

This repository is **deprecated**.

The Yoga L13 touchscreen / stylus proximity helper now lives in:

**https://github.com/ditschi/dotfiles**

- Ansible role: `ansible/roles/stylus_touch_guard`
- Feature flag: `stylus-touch-guard` (see `machine/hosts/ditschi-ThinkPad-L13-Yoga-Gen-2.yml`)

Please use `machine update` / `machine apply` on the laptop instead of installing from this repo.

Suggested GitHub action: archive this repository (Settings → Archive).
